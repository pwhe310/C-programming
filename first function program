#include <stdio.h>
#include <conio.h>
#include <ctype.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "backgroundProgram.c"

half(int i);

int main()
{
    int i;

    printf("what number do you want?");
    scanf(" %d", &i);

    half(i);

    printf("In main(), i is still %d\n", i);
    return 0;
}

half(int i)
{
    i = i/2;
    printf("your halved value is %d\n",i);
    return 0;
}
